---
run_id: L8S2-M0MD
---

# L8S2 rollout KPU

Operational record. Execute the numbered items exactly as written.

1. Use the term mappings introduced in L8S1.
2. Use the anchor: locate the last commit created before the commit whose message is "migration-complete". In that commit, open registry/L8S2_P3T_ref.md.

## Procedure card

Skill: git_history_investigation
Purpose: Navigate a repository's commit history to locate commits relative to a named commit message and read files at that revision.
Inputs: repository, anchor commit message, file path
Normalization: the last commit strictly before the named commit
Store this skill under its name. Later tasks refer to it by name and expect it to be applied without re-teaching.
